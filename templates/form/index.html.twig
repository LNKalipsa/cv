{% extends 'base.html.twig' %}

{% block title %}Ajouter au CV{% endblock %}

{% block body %}
<div class="container">
    <h1>Ajouter au CV</h1>
    {{include('form/_profil.html.twig')}}
    <div class="row mt-3">
        <div class="col-6">
            {{ include('form/_competence.html.twig') }}
        </div>
        <div class="col-6">
            {{ include('form/_leisure.html.twig') }}
        </div>
    </div>
    <div class="row mt-3 mb-5">
        <div class="col-6">
            {{ include('form/_experience.html.twig') }}
        </div>
        <div class="col-6">
            {{ include('form/_formation.html.twig') }}
        </div>
    </div>

    

    {# {{ form_start(experience_form) }}

    {{ form_end(experience_form) }} #}

</div>
{% endblock %}
