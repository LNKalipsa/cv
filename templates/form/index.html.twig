{% extends 'base.html.twig' %}

{% block title %}Ajouter au CV{% endblock %}

{% block body %}
<div class="container">

    <div class="row">
        <div class="col-11">
            <h1>Ajouter au CV</h1>
        </div>
        <div class="col-1">
            <a class="btn btn-dark" href="{{path('landing_page')}}">Accueil</a>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-6 col-md-12">
            {{ include('form/_competence.html.twig') }}
        </div>
        <div class="col-lg-6 col-md-12">
            {{ include('form/_leisure.html.twig') }}
        </div>
    </div>
    <div class="row">
        <div class="col-lg-6 col-md-12">
            {{ include('form/_experience.html.twig') }}
        </div>
        <div class="col-lg-6 col-md-12">
            {{ include('form/_formation.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
